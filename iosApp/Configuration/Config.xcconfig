TEAM_ID =
BUNDLE_ID = com.gymtracker
APP_NAME = GymTracker
FRAMEWORK_SEARCH_PATHS = $(SRCROOT)/../composeApp/build/xcode-frameworks/$(CONFIGURATION)/$(SDK_NAME)
OTHER_LDFLAGS = $(inherited) -framework ComposeApp
